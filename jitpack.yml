jdk:
  - openjdk17
before_install:
  - echo "Before Install"
  - ./ensure-java-version install
install:
  - echo "Install"
  - if ! ./ensure-java-version use; then source ~/.sdkman/bin/sdkman-init.sh; fi
  - ./gradlew build
  - ./gradlew publishToMavenLocal