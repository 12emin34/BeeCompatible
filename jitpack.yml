jdk:
  - openjdk17
before_install:
  - echo "Before Install"
  - ./ensure-java-version.sh install
install:
  - echo "Install"
  - if ! ./ensure-java-version.sh use; then source ~/.sdkman/bin/sdkman-init.sh; fi
  - ./gradlew build
  - ./gradlew publishToMavenLocal